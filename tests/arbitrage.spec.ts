import { ArbitrageInputMap, buildPathMap } from "../rules/abitrage"

describe('buildPathMap', () => {
    let poolByTokenAddress = {}
    let x = 0;
    beforeEach(() => {
        poolByTokenAddress = {};
    })

    it('should not be null', async() => {
        expect(x).toEqual(0);
    });

    //buildPathMap()
})